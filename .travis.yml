language: dart

sudo: false

dart:
  - "2.0.0"
  - stable
  - dev

cache:
  directories:
    - $HOME/.pub-cache

jobs:
  include:
    - stage: check_formatting
      script:
      - pub run bin/check_formatting.dart

stages:
  - check_formatting
